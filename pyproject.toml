[project]
name = "quant-signal"
version = "0.1.0"
dependencies = [
    "click",
    "yfinance",
    "pandas",
    "numpy",
    "scikit-learn",
    "joblib",
    "matplotlib"
]

[project.scripts]
quant-signal = "quant_signal.cli:cli"
